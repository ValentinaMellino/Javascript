//Implementare eat
//Mangiare via l'array a partire dalla posizione start fino alla posizione stop -1

//creo una funzione eat con parametri array,start,stop  (ricordare stop -1, quindi indice finale escluso)
// return (da capire)
//dichiaro la variabile array
//chiamo la funzione eat con le posizioni da mangiare

function eat(array, start, stop) {
  return; // come devo ritornare la porzione di array?
}
let array = [1, 2, 3, 4, 5, 6];
let arrayPosizioni = eat(2, 5);
console.log(arrayPosizioni);
